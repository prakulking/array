#include<stdio.h>

int main()
{
	 long int i,n, f=1;
	 printf("Enter any positive integer: ");
	 scanf("%ld", &n);
	 for(i=1;i<= n;i++)
	 {
	  f = f*i;
	 }
	 printf("%ld != %ld",n,f);
	 getch();
	 return(0);
}
